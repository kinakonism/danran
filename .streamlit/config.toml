[client]
# "Manage app" ボタンなどのツールバーを最小化
toolbarMode = "minimal"

[browser]
gatherUsageStats = false

[server]
# static/ ディレクトリを /app/static/ で配信（sw.js・manifest.json・icons）
enableStaticServing = true

