[project]
name = "danran"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "bcrypt>=5.0.0",
    "streamlit>=1.57.0",
    "streamlit-autorefresh>=1.0.1",
    "supabase>=2.30.0",
]

[dependency-groups]
dev = [
    "playwright>=1.60.0",
]
